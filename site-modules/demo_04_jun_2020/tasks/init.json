{
	"description": "Show task defaults in the console",
  "parameters": {
     "bool": {
       "default": true,
       "type": "Boolean",
       "description": "should i do a thing?"
     },
     "string": {
       "default": "true",
       "type": "String",
       "description": "string theory"
     },
     "array": {
       "default": ["one", 1, true],
       "type": "Array",
       "description": "is it list or array?"
     },
     "hash": {
       "default": { "foo": ["bar"] },
       "type": "Hash",
       "description": "breakfast hash"
     },
    "int": {
      "default": 1,
      "type": "Integer",
      "description": "INTerplanetary"
    },
    "required": {
    	"description": "required parameter",
    	"type": "Any"
    }
  }
}
